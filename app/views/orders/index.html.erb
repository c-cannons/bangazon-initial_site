<%= render template: "./shared/_header" %>

<div class="container center">
  <div class="row justify-content-center">
    <div class="col-md-8">

      <h2>PUTTING THE CART BEFORE THE HORSE</h2>

      <div>
        <% @shopping_cart.each do |item| %>
          <p>Item name: <%= item.product_name %>; Price: <%= item.product_price %>; Description: <%= item.product_desc %>
          </p>
        <% end %>
      <div>

    </div>
  </div>
</div>
