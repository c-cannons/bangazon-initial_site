<%= render template: "./shared/_header" %>

<div class="container center">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <br>
      <h2>Cart</h2>
      <br>
\

      <br><br>


      <%= form_with scope: :order_detail, url: order_details_path, local: true do |form| %>

        <% if @order_detail.errors.any? %>
          <div id="error_explanation">
            <h2>
              <%= pluralize(@order_detail.errors.count, "error") %> prohibited this action from being taken:
            </h2>
            <ul>
              <% @order_detail.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <p>
         <h2>SOME STUFF GOES HERE</h2>
        </p>

      <% end %>

    </div>
  </div>
</div>
